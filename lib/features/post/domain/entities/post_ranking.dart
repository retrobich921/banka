/// Способ ранжирования постов в разделе «Топы».
enum PostRanking {
  /// По итоговому баллу оценки (поле `ratingScore`, посты без оценки не входят).
  topRated,

  /// По числу лайков (`likesCount`).
  mostLiked,
}
