rules_version = '2';

// Sprint 2 baseline — всё под auth. Загрузки фото банок настраиваются в Sprint 7.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
